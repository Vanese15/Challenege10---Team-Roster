// TODO: Include packages needed for this application
const fs = require("fs");
const inquirer = require("inquirer");
const Engineer = require("./lib/Engineer");
const Manager = require("./lib/Manager");
const Intern = require("./lib/Intern");

const teamAppMaker = () => {
    
const teamGenerator = () => {
    return inquirer.prompt(userInput)
}}

const userInput = [
    inquirer.prompt([{
        type: "list",
        message: "What type of employee would you like added to your team?",
        name: "addEmployeePrompt",
        options: ["Manager", "Engineer", "Intern"]
        }]).then(userInput) {
            
        }



]




    

// TODO: Create an array of questions to generate roster


// TODO: Create a function to generate HTML


// TODO: Create a function to initialize app


// Function call to initialize app